# Claims for the knowledge map (sky.html). Source of truth for assets/sky-data.js —
# regenerate with `python3 sky-data.py`; check.py fails if the output is stale.
#
# Format:  ## <id> | <display name> | <related ids, comma-separated>
#          <kind>|<claim text>
# Kinds:   m memory · s skill · f fact · p preference · c convention
#
# Every line here is visible copy: check.py scans it with the same vocabulary and
# phase-language gates as the pages. Plain words, one sentence, no internal names.

## payments | Payments & billing | auth, compliance, queues, support
m|the payments sandbox rate-limits per API key, not per account — space retries 2 s apart, not 30
f|card networks require the merchant descriptor to match the legal entity name within 22 characters
c|every money amount crosses a service boundary as integer minor units plus an ISO 4217 code, never a float
m|processor webhooks arrive out of order under load; an invoice can be paid before it is created
s|refund-reconcile: matches processor settlement files against the ledger and lists unmatched rows by day
f|3-D Secure exemptions for low-value transactions apply under €30 and reset after five consecutive exemptions
p|finance prefers refunds issued to the original payment method; store credit only when the method is closed
c|idempotency keys on charge requests are the order id plus attempt number, and they live for 24 hours
m|the EU acquirer rejects descriptors containing an ampersand — the checkout for two brands broke on that alone
f|SEPA direct debit returns can arrive up to 8 weeks after collection; revenue is recognized on settlement, not authorization
s|dunning-runbook: the retry schedule for failed renewals — day 1, 3, 7, 14 — and which email each step sends
m|the tax engine treats a missing postal code as a lookup failure, not as unknown, and blocks checkout silently
c|never log a full card number or a CVV, even masked to the last four; the masking helper is the only path
f|Apple in-app purchases settle in the store's currency; the exchange rate is fixed at the time of the receipt
p|the billing team wants proration computed on seconds, not days, after the leap-day dispute
m|a customer with two subscriptions and one card sees a single consolidated invoice only if both share the billing anchor
s|invoice-diff: explains why two invoices for the same plan differ, line by line, including tax and credits
f|chargeback response windows are 7 to 45 days depending on the network, and evidence must be plain text or PDF
c|price changes are new price objects; existing subscriptions are migrated explicitly, never by editing the price
m|the annual-plan discount was applied twice when a coupon and a promotion overlapped — the coupon stack is now capped at one
f|strong customer authentication fails for saved cards off-session unless the mandate was set up on-session
p|support prefers the invoice PDF to show the purchase order number above the line items, not in the footer
s|settlement-forecast: estimates cash arrival by acquirer, currency and weekday from the last 90 days of payouts
m|payout delays on Mondays are the acquirer batching Friday's captures, not a failure — wait until 14:00 before escalating
c|a free trial converts on the anchor date at 00:00 in the customer's billing timezone, not the server's
f|VAT reverse-charge applies to EU business customers with a validated VAT id; validation is cached for 30 days
m|the checkout retry button resubmitted the same intent and produced a duplicate hold; it is disabled while a confirmation is pending
s|ledger-audit: proves every processor event has exactly one ledger entry and reports the gaps
p|the CFO wants deferred revenue reported by contract start date, not by invoice date
f|refunds after 180 days go back as a credit memo because most networks refuse the original transaction by then
c|coupon codes are case-insensitive on entry and stored uppercase; the legacy mixed-case codes were migrated
m|the failed-payment email rendered the amount in the merchant currency, which confused customers billed in USD
f|a partial capture releases the remainder of the authorization immediately with most acquirers, but not with all of them
s|tax-jurisdiction-check: resolves an address to the tax authorities that apply and the rates in force on a date
m|usage-based invoices need the meter closed before the invoice is finalized, or late events land on the next period
p|the team prefers processor-hosted checkout pages for new markets until volume justifies a native form
c|every refund carries a reason code from the fixed list; free-text reasons broke the monthly finance report
f|the network token replaces the card number for recurring charges and survives card reissue
m|cancelling at period end and then reactivating created a second subscription; reactivation must reuse the original
s|payments-smoke: runs one authorization, capture, refund and dispute in the sandbox and checks each webhook arrived

## auth | Authentication & identity | payments, security, api, mobile
f|access tokens live 15 minutes, refresh tokens 30 days, and a refresh rotates the whole token family
m|the identity provider lowercases email addresses on login but the user table preserved case — two accounts for one person
c|service-to-service calls carry a short-lived signed token from the workload identity, never a shared API key
s|session-revoke: invalidates every session for a user across regions and confirms from each region's cache
f|SAML assertions are validated against the exact audience string; a trailing slash is a different audience
p|the security team prefers passkeys as the default second factor and SMS only as a documented exception
m|the login page rate limit counted by IP and locked out an entire office behind one NAT address
c|permission checks happen in the service that owns the resource, not at the API gateway
f|OAuth refresh tokens issued to public clients are single-use; replaying one revokes the whole chain
s|permission-diff: shows what a role can do today versus after a proposed change, per resource
m|the SCIM provisioning connector deactivates users but never deletes them — reactivation restores their groups
f|a token's key id names the signing key; keys rotate monthly and the previous key stays valid for 48 hours
c|impersonation by support staff is logged with both identities and expires after 30 minutes
p|product wants social login to link to an existing account by verified email, never to create a duplicate
m|the password reset link was valid for 24 hours; it is 20 minutes since the shared-mailbox incident
f|the corporate identity provider sends group membership as names, not ids, and group names have been renamed twice
s|token-inspect: decodes a token, checks its signature against the current key set and explains any rejection
m|two services validated tokens against a cached key set that never refreshed and rejected everything after rotation
c|new roles are defined in the roles repository and reviewed by the owning team; nobody creates a role in the admin UI
f|device-bound sessions on mobile survive app updates but not a restore to a new device
m|the admin console accepted an expired session for read-only pages because the check ran only on writes
p|the platform team prefers one identity provider per environment over a shared tenant with environment claims
s|sso-onboard: configures a customer's identity provider from its metadata URL and runs the test login
f|a service account key unused for 90 days is disabled automatically on the first of the month
m|the mobile app stored the refresh token in shared preferences; it moved to the keystore after the audit finding
c|multi-factor enrollment is required within 7 days of account creation for anyone with production access
f|account lockout is 10 failed attempts in 15 minutes, then a 15-minute cooldown, per account rather than per IP
m|the audit log wrote the user's email into the message body, which the retention job then could not redact
p|customers prefer a single sign-on link in the invitation email over a temporary password
s|login-anomaly-report: lists sign-ins from a new country or a new device family for a tenant in the last 24 hours
f|the identity provider's token endpoint returns the same error code for a wrong secret and a revoked client
c|an API key belongs to a workspace, not a person; a person leaving does not rotate it, a role change does
m|the consent screen listed scopes by internal name; customers read read:ledger as reading their bank ledger
f|browsers partition third-party cookies, so the embedded widget authenticates with a token in the page, not a cookie
m|the login redirect allowed any subdomain and one abandoned subdomain pointed at an unclaimed bucket
c|redirect URLs are matched exactly against the registered list; wildcards are refused in review
s|key-rotation-drill: rotates the signing key in staging and verifies every consumer picks up the new key in time
f|WebAuthn credentials are bound to the origin; a domain move requires customers to re-register their keys
p|the team prefers denying by default and logging the denied permission over allowing with a warning
m|the health endpoint sat behind authentication and the load balancer marked every instance unhealthy after a key rotation

## data | Data platform | analytics, ml, databases, cloud
f|the warehouse partitions by event date in UTC; a query filtering on local date scans every partition
m|Spark's default of 200 shuffle partitions produced two hundred tiny files per hour; adaptive execution fixed it
c|tables are named domain, entity, grain, in that order, and a table without a documented owner is dropped after 90 days
s|table-lineage: walks a table's upstream sources to the raw layer and lists every job in between
f|Delta time travel keeps 7 days of history by default; the finance tables keep 400 days by policy
p|analysts prefer one wide fact table per domain over a snowflake of dimension tables
m|the nightly job read a table while it was being overwritten and produced half a day of revenue; use replace-where, not overwrite
f|small files under 32 MB dominate read time; compaction runs weekly on tables with more than 1,000 files
c|schema changes are additive; a column is deprecated for one quarter before it is dropped
s|freshness-check: compares each table's latest partition against its declared arrival deadline and pages the owner
m|joining on a string customer id was 40× slower than on the integer surrogate; the surrogate is mandatory in joins now
f|Z-ordering on the two most filtered columns cut the customer-lookup scan from 30 GB to 900 MB
p|the data team prefers SQL for transformations and Python only where the SQL becomes unreadable
m|the cluster autoscaled to 40 nodes to serve one skewed join; salting the hot key brought it to 8
c|raw data is never modified; corrections are applied in the silver layer with a reason column
f|broadcast joins are safe under 10 MB per table on the current worker size; above that they spill
s|cost-by-job: attributes cluster spend to jobs and owners for the month, with a week-over-week delta
m|a timestamp column without a timezone was read as local by one client and UTC by another; the column carries the zone now
f|the streaming job checkpoints every 30 seconds; a checkpoint older than the retention window cannot resume
c|every pipeline declares its expected row count range; a run outside the range fails rather than publishes
m|null-safe equality in Spark is a separate operator, and the deduplication job dropped every row with a null key until it used it
p|the platform team prefers catalog grants on schemas, not on individual tables
s|backfill: reruns a pipeline for a date range in parallel, ordered by partition, with a dry-run row-count report
f|change capture from the order database arrives with a 5-minute lag; downstream dashboards state that lag on the page
m|the deduplication window of one hour missed replayed events from a 3-hour producer outage; it is 24 hours now
c|personal-data columns are tagged at ingestion and masked in every view outside the restricted schema
f|the warehouse's SQL endpoint caches results for 24 hours; a repeated dashboard query is free within the day
m|a distinct count on 2 billion rows ran 40 minutes; the approximate count is accurate within 2% and runs in one
s|schema-drift-report: lists columns added, removed or retyped in a source since the last snapshot
f|external tables over object storage list files on every query; converting to managed tables removed the listing cost
p|finance prefers the reporting layer rebuilt nightly rather than updated incrementally, for auditability
m|the job collected a whole table to the driver to check emptiness and pulled 12 GB; limit 1 does the same in milliseconds
c|a dataset is not published until its documentation page has a description, an owner and a freshness contract
f|liquid clustering replaces manual partitioning for tables under continuous write and adapts as the filters change
m|two teams computed active users with different windows and reported figures that differed by 11% to the board
s|metric-registry-check: compares a dashboard's SQL against the registered metric definition and flags divergences
f|the file listing on a 100,000-partition table took longer than the query; the table was repartitioned by month
c|joins across domains use the shared conformed dimensions, never a domain's private copy
m|a retry on a failed write appended the batch twice; writes are idempotent on batch id since then
p|the team prefers column-level lineage in the catalog over hand-maintained data dictionaries

## cicd | Build, CI & release | testing, devenv, cloud, mobile
f|the main pipeline runs in 11 minutes; the slowest stage is integration tests at 6, sharded 4 ways
c|every merge to main is deployable; a change that is not ready ships behind a flag, never on a branch
m|the cache key included the runner's OS version and invalidated every build after an image update
s|release-notes: assembles the changelog from merged pull requests since the last tag, grouped by area
f|container images are built once per commit and promoted between environments by digest, never rebuilt
p|the platform team prefers trunk-based development with branches that live under two days
m|a flaky test was retried automatically for three months and hid a real race condition
c|a pipeline that touches production requires two approvals from the owning team's reviewer list
f|deployments roll out to 5% of instances for 10 minutes before continuing; an error-rate rise halts the rollout
s|rollback: redeploys the previous digest to an environment and verifies health, without rebuilding
m|the deploy job used the latest tag of the base image and a silent upstream change broke TLS in production
f|build artifacts are kept for 90 days; release artifacts are kept for the life of the supported version
c|version numbers are semantic; the major bumps on any breaking change to a public API or a config file
p|mobile release managers prefer a release branch cut on Monday and frozen on Wednesday
m|parallel jobs shared a workspace directory and overwrote each other's build outputs intermittently
f|dependency updates arrive as one grouped pull request per week; security updates arrive immediately
s|pipeline-bisect: finds the commit that made a pipeline stage slower by replaying it over a commit range
m|the pipeline had permission to write to every environment; scoping the token to one stopped a wrong-environment deploy
c|a release is a signed tag; the artifact's provenance attestation names the commit, the builder and the inputs
f|the monorepo's affected-target detection cuts the average pipeline to a third of the full build
m|secrets appeared in the log when a debug flag echoed the environment; masking is enforced now
p|the team prefers failing fast on lint and type errors before any test job starts
s|env-promote: promotes a verified digest from staging to production with the change list attached to the ticket
f|the build cache is shared across branches by content hash and hits 80% on a typical pull request
m|a merge queue removed the stale-branch failures that used to break main twice a week
c|database migrations run in their own pipeline step before the application deploys, and they are backward compatible
f|canary analysis compares the canary's latency and error rate against the baseline over 10 minutes with a fixed threshold
m|the release checklist lived in a wiki page nobody updated; it is a pipeline stage that blocks now
p|the reliability team prefers deploys between 09:00 and 16:00 local, never on the last working day before a holiday
s|deploy-freeze: declares a freeze window for an environment and rejects deploys with the reason and the contact
f|a hotfix branches from the release tag, ships, and is merged back to main the same day
m|the pipeline pulled a dependency from a public registry that was taken down; everything comes from the internal mirror now
c|every job pins its tool versions; a floating latest tag is refused by the pipeline linter
f|build minutes cost more than the engineering time saved below a 4-minute pipeline; that is the target, not zero
m|the test database was reused between runs and a leftover row made a test pass that should have failed
s|feature-flag-audit: lists flags older than 60 days with their owners and whether they are still read anywhere
f|a deployment that changes environment variables restarts every instance; a config-only change uses the reload endpoint
m|rollback took 25 minutes because it rebuilt the image; redeploying the previous digest takes 90 seconds
c|the pipeline configuration lives with the code it builds and changes through the same review
p|the team prefers one pipeline definition shared through templates over per-repository copies

## observability | Observability & on-call | support, cloud, queues, databases
f|the alert threshold for checkout errors is 0.5% over 5 minutes; below that, a ticket, not a page
m|the dashboard averaged latency across regions and hid a 4-second p99 in one of them; percentiles are per region now
c|every service emits the request id in every log line and forwards it downstream in the header
s|incident-timeline: builds a timeline from alerts, deploys and chat messages for an incident window
f|metrics retention is 15 months at one-minute resolution; traces are sampled at 10% and kept for 14 days
p|on-call engineers prefer alerts that name the runbook step, not the metric that crossed the line
m|a log line per request at info level cost more than the service's compute; it is one structured line per request now
c|a page must be actionable by the person paged; anything else is a ticket routed to the owning team
f|the synthetic check runs from three regions every minute and requires two of three to fail before paging
s|slo-report: computes the error budget burn for each service over the last 28 days and projects when it runs out
m|the disk-full alert fired at 90% on a 4 TB volume with 400 GB free; alerts are on time-to-full now, not percent
f|traces cross the queue boundary only when the producer copies the trace context into the message headers
c|dashboards are code in the observability repository; a dashboard edited in the UI is overwritten on the next deploy
p|the team prefers histograms to summaries for latency because they aggregate across instances
m|the health check returned 200 while the database connection pool was exhausted; it checks a real dependency now
f|alert fatigue is measured: more than four pages per on-call night triggers a review of the alert rules
s|noisy-alert-report: ranks alerts by how often they fired without a corresponding incident in the last 30 days
m|the log pipeline dropped lines over 32 KB silently; stack traces from one service never arrived
c|every alert has an owner team, a runbook link and a severity; the linter rejects an alert missing any of them
f|the status page updates automatically from the service-level state; a human adds the words, not the color
m|two services shared a metric name with different labels and the sum was meaningless for a quarter
p|the reliability team prefers exemplars linking a latency spike to a trace over a separate slow-query log
s|dependency-map: draws which services called which in the last hour from trace data, with error rates on the edges
f|the on-call rotation is one week, handed over on Tuesday at 10:00, with a written handover note
m|the alert on queue depth paged during every planned batch; it checks depth against the consumer's rate now
c|a postmortem is written within 5 working days, blameless, and its action items have owners and dates
f|cardinality limits are 10,000 series per metric; a user id as a label blows through that in an hour
m|a clock skew of 40 seconds on one host made its traces appear to finish before they started
s|log-sample: pulls a representative sample of a service's logs for a time window and groups by template
f|the paging path does not depend on the company's own infrastructure; it goes through the vendor's app and SMS
m|the deploy marker was missing from the latency dashboard; every incident review started by asking what changed
c|severity 1 means customers cannot complete a core task; severity is set by impact, not by who is awake
p|the team prefers a single wide event per request over many narrow log lines
f|the error budget policy freezes feature deploys for a service that has burned its budget until it recovers
m|the retention job deleted the traces from the incident before the review; incident windows are pinned now
s|runbook-check: verifies every command in a runbook still exists and every link resolves
f|synthetic transactions use a dedicated test tenant whose data is excluded from every business metric
m|an alert on absent data was missing, so a dead exporter looked like a perfectly healthy service for two days
c|the on-call handover lists open incidents, silenced alerts and their expiry, and anything expected to fire
p|support prefers the incident channel to post a customer-facing summary every 30 minutes, even if nothing changed

## cloud | Cloud infrastructure & cost | cicd, security, observability, data
f|the production account budget is €140k a month; anything above 90% of forecast pages the platform lead
m|an idle development cluster ran for six weeks at €3,000 a week because nobody owned it; clusters carry an owner tag now
c|every resource carries owner, service and environment tags; the nightly job stops anything untagged in dev
s|cost-anomaly: flags a service whose daily spend departs from its 30-day pattern and names the resource type
f|reserved capacity covers 70% of steady-state compute; the rest runs on demand or spot
p|the platform team prefers managed databases over self-hosted, and accepts the 30% premium for it
m|cross-zone traffic was a fifth of the bill because the cache and its clients were in different zones
c|infrastructure changes go through the plan-and-apply pipeline; a change made in the console is reverted on the next apply
f|spot interruptions arrive with a two-minute notice; the batch scheduler drains a node on the notice
s|rightsize: recommends instance sizes from 14 days of utilization and estimates the saving per service
m|the storage bill doubled from versioned buckets keeping every overwritten object; lifecycle rules expire versions after 30 days
f|egress to the internet costs about 20× more than egress within the region; the CDN fronts every public download
c|a new environment is created from the environment module, never by copying another environment's configuration
p|finance prefers cost reports by product line, which needs the product tag on every shared resource
m|a NAT gateway carried all outbound traffic from the data pipeline and cost more than the pipeline's compute
f|snapshots older than 35 days are deleted unless tagged retain, and the retain tag needs a ticket id
s|orphan-finder: lists volumes, addresses and load balancers attached to nothing, with their monthly cost
m|the load balancer's idle timeout was shorter than the client's keepalive and every 60th request failed
c|production and non-production run in separate accounts; a role in one cannot assume a role in the other
f|object storage lifecycle moves objects to the infrequent tier after 30 days and to archive after 180
m|a Kubernetes request of 4 CPU per pod on a workload that used 0.3 wasted two-thirds of the cluster
p|the team prefers autoscaling on a queue-depth metric over CPU for the batch workers
s|budget-forecast: projects the month's spend from the run rate and lists the three fastest-growing services
f|the DNS time-to-live for service records is 60 seconds; a failover takes at most that plus the health check interval
m|the recovery region was configured but never exercised; the first drill found a missing secret and two hard-coded region names
c|every quarter one service is failed over to the secondary region during business hours, and the result is published
f|the private endpoint for object storage removes the gateway hop and the egress charge for in-region traffic
m|an unbounded log group cost more than the service that wrote to it; log groups have a retention setting by policy
s|tag-compliance: reports resources missing required tags by account and owner, with a link to fix each
f|the compute savings plan commitment is reviewed quarterly against the trailing utilization
m|the autoscaler scaled on average CPU while one hot instance saturated; it scales on p90 per instance now
c|images are rebuilt weekly from the hardened base, and an instance older than 30 days is replaced
p|the security team prefers private subnets with a bastion over any instance with a public address
f|GPU instances are scheduled through the shared queue; a team cannot reserve a GPU node for itself
m|a development database restored from a production snapshot carried customer data for a week; restores are masked now
s|drift-report: compares deployed infrastructure against its definition and lists what differs and who changed it
f|the interconnect to the office is 10 Gbit and saturates during the nightly backup unless it is rate-limited
m|the cluster upgrade evicted pods without disruption budgets and took the checkout down for four minutes
c|a cost-affecting change over €500 a month states the estimate in the pull request description
p|the platform team prefers one cluster per environment over namespaces separating environments in one cluster

## security | Security & secrets | auth, cloud, compliance, devenv
c|secrets come from the vault at start-up; an environment variable holding a secret fails the deployment linter
m|a token committed to a private repository was found by a scanner within eleven minutes of the push
f|the vulnerability scanner runs on every image build; a critical finding blocks promotion to production
s|secret-scan: finds credentials in a repository's history and reports the commit, the file and the age
p|the security team prefers short-lived credentials from the workload identity over any long-lived key
m|the internal admin tool trusted the forwarded-for header and anyone could spoof an office address
c|dependencies are pinned by hash; a lock file change is reviewed by a second person
f|TLS 1.2 is the minimum everywhere; the last client on 1.1 was a payment terminal retired in March
m|a debug endpoint exposed the process environment, including the database password, on one service for a month
s|dependency-audit: lists direct and transitive dependencies with known vulnerabilities and the version that fixes each
f|the bug bounty pays out within 30 days; the most common valid report is an authorization gap on an internal id
c|every public endpoint has a rate limit; the default is 100 requests a minute per token
m|the file-upload endpoint accepted SVG and an SVG carried a script; uploads are re-encoded to PNG now
p|the team prefers allow-lists for outbound network access from production; the default is deny
f|the security review for a new service takes about a week and starts from the threat model, not the code
s|threat-model-template: produces the data-flow diagram and the questions the review will ask, from the service description
m|the CI runner had a wildcard permission that let a test job read every secret in the organization
c|production access is through the bastion with a recorded session; a direct SSH key is removed within a day of discovery
f|the web application firewall blocks the known injection patterns; it is defense in depth, not the control
m|a signed URL for a private file was valid for 7 days and got pasted into a public ticket; they are valid for 15 minutes now
s|permission-boundary-check: verifies no role in the account can escalate to administrator through a policy chain
f|browser security headers are set at the edge: content security policy, frame denial and strict transport security
c|a vulnerability report from outside gets an acknowledgement within one working day and a fix timeline within five
m|the internal package registry accepted a package name that shadowed a public one; names are reserved now
p|the security team prefers a hardware key for anyone with production or billing access
f|encryption at rest is on every store by policy, with keys in the managed key service and a yearly rotation
m|the backup bucket had a public-read policy from a test years ago; public access is blocked at the account level now
s|exposure-check: probes the public attack surface for open ports, expired certificates and default pages
f|a leaked credential is rotated first and investigated second; rotation is a runbook step, not a decision
c|logs never carry passwords, tokens or full card numbers; the log library redacts the known field names
m|the password hashing cost was set in 2016 and a modern GPU cracked the test set in hours; the cost factor is tuned yearly
f|the incident response plan names a coordinator, a communications lead and a scribe for every security incident
m|a request forgery through the webhook tester reached the metadata endpoint; outbound calls go through the egress proxy now
s|cert-expiry-report: lists every certificate the organization serves with its expiry and its owner
p|the platform team prefers signing artifacts in the pipeline over signing on a developer's machine
f|a new password is checked against the breached-password list at set time and again at login
c|third-party scripts on the checkout page are reviewed quarterly and pinned by hash
m|a service printed the full request body on a parse error and logged customers' documents for two weeks
f|access reviews run quarterly; a permission not confirmed by its owner within two weeks is removed
p|the team prefers to disclose an incident affecting customers within 72 hours, ahead of the legal deadline

## api | API design & versioning | auth, frontend, mobile, queues
c|breaking changes ship as a new major version at a new path; the old version is supported for 12 months
f|list endpoints paginate with an opaque cursor; page numbers were removed because inserts shifted the pages
m|a field renamed in a minor release broke three customer integrations; renames are additions plus a deprecation now
s|api-diff: compares two versions of the specification and classifies each change as breaking or compatible
p|the platform team prefers one resource per endpoint over an endpoint that returns a graph of related objects
f|timestamps are RFC 3339 in UTC with a Z suffix; the one endpoint that returned local time is deprecated
c|every error response has a machine-readable code, a human message and a request id
m|the rate limit headers used a Unix timestamp on one endpoint and seconds-to-reset on another; all use seconds now
f|idempotency keys are accepted on every POST that creates something and are stored for 24 hours
s|sdk-regenerate: regenerates the client libraries from the specification and opens one pull request per language
m|a nullable field returned as absent in one version and null in another; both are documented as equivalent now
c|the specification is the source; handlers are validated against it in tests, and the documentation is generated from it
f|webhooks are signed with a shared secret and a timestamp; receivers reject anything older than 5 minutes
p|customers prefer a fields parameter to trim responses over separate lightweight endpoints
m|an enum gained a value and a strict client crashed; clients are told to treat unknown enum values as a default
f|the default page size is 50 and the maximum is 200; asking for more returns the maximum, not an error
c|a deprecated field carries a deprecation header and a sunset date; usage is reported to the owning team weekly
m|the bulk endpoint returned 200 with partial failures in the body and clients treated it as success
s|breaking-change-scan: finds clients still calling a deprecated version from the gateway logs and names the tenants
f|the sandbox and production share the specification and differ only in the base URL and the credentials
m|the search endpoint accepted a regular expression and a customer's pattern ran for 40 seconds per request
p|the team prefers explicit expand parameters to embedding related objects by default
c|money is an object with the amount in minor units and a currency; a bare number for money fails review
f|the gateway adds a retry-after header on every 429 and 503, and the clients honor it
m|a client sent 10,000 requests a second in a retry storm; the SDKs use exponential backoff with jitter since
s|contract-test: replays recorded customer requests against a candidate build and reports any changed response
f|long-running operations return 202 with a status URL; polling more than once a second gets a 429
m|the updated-at field changed on every read because a counter lived in the same row; it is a separate column now
c|resource ids are opaque strings with a type prefix, and clients are told not to parse them
p|the developer relations team prefers examples that run as-is with a copied key over abstract snippets
f|GraphQL is offered for the dashboard only; the public API is REST, and that split is documented
m|a webhook retry policy retried for 3 days and delivered an event to a customer who had already handled it by hand
s|webhook-replay: resends selected events to a customer endpoint with a marker header, on request
f|the API returns 404, not 403, for a resource the caller cannot see, so ids cannot be enumerated
m|the response included an internal database id next to the public id and customers built on the wrong one
c|filter parameters follow one grammar across endpoints; a new filter is added to the grammar, not to one endpoint
p|the team prefers a versioned specification in the repository over a versioned documentation site
f|the changelog is generated from the specification diff and published with every release
m|the batch endpoint had no size limit and a 50 MB request took the service down; the limit is 1,000 items
s|spec-lint: checks the specification against the style rules and blocks a merge on a naming violation

## frontend | Frontend & design system | api, testing, analytics, mobile
c|colors, spacing and type come from the design tokens package; a hex value in a component fails review
m|the date picker used the browser locale for display and the server's for parsing, off by one day in three regions
f|the bundle budget is 180 KB compressed for the first route; the check runs on every pull request
s|a11y-audit: runs the accessibility checks on every route and reports contrast, labels and focus order
p|the design team prefers one primary action per screen and a text button for everything else
m|the modal trapped focus but did not return it on close; keyboard users landed at the top of the page
f|every interactive element has a visible focus ring; the reset stylesheet that removed it is gone
c|components are documented in the component catalog with their states before they are used in a product
m|the infinite scroll list re-rendered every row on each append; virtualization brought it under 16 ms
f|the design system supports right-to-left layouts; logical properties are used, never left and right
s|token-diff: shows which components change when a token value changes, before the change ships
m|a font loaded from a third-party host blocked rendering for 1.2 seconds on slow connections; fonts are self-hosted now
p|product prefers skeleton placeholders to spinners for anything that loads within two seconds
c|forms validate on blur and again on submit; error messages sit next to the field, not at the top
f|images ship in modern formats with width and height attributes so the layout does not shift on load
m|the tooltip closed on hover-out and mouse users could not reach the link inside it
s|visual-regression: renders every catalog story and compares against the approved screenshots
f|the dark theme is a token set, not a stylesheet; components never check which theme is active
m|state was held in the URL for filters but not for sort, and a shared link lost the sort every time
c|text is never in an image; the one exception, the wordmark, carries the words in its alt text
p|the team prefers server-rendered pages with islands of interactivity over a client-rendered application
f|the minimum touch target is 44 by 44 points, and the linter measures the rendered size, not the padding
m|a third-party chat widget added 400 KB and three trackers; it loads on request now, not on every page
s|bundle-report: breaks the first-route bundle down by package and flags anything above 20 KB that arrived this week
f|animations respect the reduced-motion preference; a component that animates unconditionally fails the catalog check
m|a table with 40 columns was unreadable on a phone; the table shows the six key columns and expands on tap
c|copy in components comes from the localization files; a hard-coded string is a build error
p|the design team prefers sentence case everywhere, including buttons and menu items
f|the color contrast floor is 4.5:1 for text and 3:1 for large text and interface parts
m|the toast notifications stacked forever during a batch operation; they collapse into a count now
s|component-usage: lists where each component is used across products and which version each product is on
f|the browser support matrix is the last two major versions of the evergreen browsers and the current Safari
m|a full-height hero was cut off by the mobile browser's toolbar; the small-viewport units fixed it
c|icons come from the icon set and carry an accessible name or are hidden from assistive technology
p|product prefers destructive actions to require a typed confirmation, not just a second click
f|date and number formatting uses the platform's locale APIs; the hand-written formatter was retired
m|the search input debounced at 100 ms and sent a request per keystroke for fast typists; it is 300 ms now
s|storybook-coverage: lists components without a documented error, loading or empty state
f|the print stylesheet hides navigation and expands collapsed sections; invoices are printed from it
c|a component is deprecated with a codemod that migrates its uses; deprecation without a codemod is refused

## mobile | Mobile apps | api, cicd, auth, frontend
f|the iOS review takes one to three days; a release train is cut on Monday to submit by Wednesday
m|the app read the device clock for token expiry and a user with a wrong clock was logged out on every launch
c|every network call goes through the API client, which handles retries, auth refresh and offline queuing
s|crash-triage: groups the week's crashes by stack signature and links each to the owning team
p|the mobile team prefers feature flags with a remote kill switch for anything touching payments
f|Android background execution is limited; a sync is scheduled through the work manager, never a timer
m|the app bundle grew 40 MB from unoptimized images in three months; assets are compressed on import now
c|the minimum supported OS versions are the last three majors; dropping one is announced a release ahead
f|push notification tokens rotate; the app re-registers on every launch and the server keeps the latest per device
s|store-release: uploads the build, fills the release notes from the changelog and starts a staged rollout at 10%
m|a staged rollout at 100% on day one shipped a login crash to every user; rollouts hold at 10% for 24 hours
f|deep links are verified with the association file on the domain; an unverified link opens the browser
p|design prefers the platform's native navigation patterns over a shared custom navigation
m|the offline queue replayed a payment twice after reconnect; queued writes carry an idempotency key now
c|secrets never ship in the binary; the app fetches configuration after authentication
f|certificate pinning is on for the API host with a backup pin; a rotation without updating the backup bricks old builds
m|the app requested location on first launch and 30% granted it; asking in context raised that to 70%
s|permission-audit: lists the permissions each build requests and flags any new one since the last release
f|accessibility labels on custom controls are required; the store review rejected a build without them once
m|the tablet layout was a scaled phone layout; a two-column layout above 600 points doubled tablet session length
c|analytics events are defined in the shared event schema; an undefined event name fails the build
p|the team prefers a single cross-platform codebase for business logic and native views on each platform
f|the app's cold start budget is 1.5 seconds on the reference low-end device
m|a synchronous disk read on the main thread froze the launch screen on older phones
s|startup-trace: profiles a cold start on the reference device and attributes time to initializers
f|in-app purchases restore across devices through the store account; the server verifies the receipt every time
m|the app cached the API response with the user's token inside it and a shared device showed one user's data to another
c|a release is tested on the three most common devices and the reference low-end device before submission
p|users prefer biometric unlock after the first login; the setting defaults on where the device supports it
f|the store listing screenshots are generated from the app in each locale; the hand-made ones drifted twice
m|the in-app browser opened external links inside the app and users could not get back; external links open the browser
s|localization-check: finds untranslated strings and layout overflows in every supported locale
f|background refresh runs at most every 15 minutes and is skipped on low battery by the operating system
m|a debug menu was reachable in a release build through a seven-tap gesture; debug code is compiled out now
c|crash-free sessions stay above 99.8% for a build to continue its rollout
p|the team prefers a forced update only for security fixes; everything else nudges with a dismissible banner
f|the app supports dynamic type; a layout that breaks at the largest accessibility size fails review
m|the app queued analytics events offline without a cap and one device uploaded 2 GB on reconnect
s|api-compat-check: verifies the current builds in the field still work against the API about to be released
f|app updates are staged through the store's release tracks: internal, closed, open, then production
## databases | Databases & storage | data, observability, api, cloud
f|the primary database fails over in about 40 seconds; connection pools reconnect on the first error, not on a timer
m|an index on a low-cardinality status column was never used; the planner preferred a scan, and the index cost write time for a year
c|every table has a created-at and an updated-at column in UTC, set by the database, never by the application
s|slow-query-digest: groups the day's slow queries by normalized shape and ranks them by total time
f|the connection pool is 20 per instance; more connections than cores times four made the database slower, not faster
p|the platform team prefers migrations that add a nullable column, backfill in batches, then add the constraint
m|a migration added a not-null column with a default and locked the orders table for eleven minutes
f|read replicas lag by under a second normally and by minutes during a bulk load; reads that need their own writes go to the primary
c|foreign keys are enforced in the database; the application layer check alone let orphans in twice
s|index-advisor: proposes indexes from the query log and estimates the write cost of each
m|the soft-delete flag was forgotten in one query and deleted customers appeared in an export
f|a transaction open for more than 30 seconds is killed by the watchdog; long work happens in batches
p|the data team prefers UUIDv7 primary keys because they sort by time and do not fragment the index
c|a query in application code carries a comment with the caller's name, so the slow-query log points at a file
m|the count of a 300-million-row table for a dashboard ran every minute; an approximate count from statistics serves it
f|the backup runs at 02:00 UTC and a restore to a point in time takes about 20 minutes per 100 GB
s|restore-drill: restores last night's backup into an isolated instance and verifies row counts against production
m|the restore drill found that the backup was encrypted with a key nobody could find; the key lives in the vault with the runbook now
c|schema changes are reviewed by the database owners for lock behavior before they are reviewed for correctness
f|the cache in front of the product catalog has a 60-second time-to-live and a 97% hit rate
m|a cache stampede on expiry sent 4,000 identical queries in one second; the entry is refreshed ahead of expiry now
p|the team prefers one database per service; a shared schema between two services is a merge, not an integration
f|large text and binary content lives in object storage with a pointer in the row; the row stays small
s|table-growth-report: lists the ten fastest-growing tables and projects when each reaches its partition limit
m|the sequence for order numbers was reset by a restore into staging and then copied into production
c|a delete of more than 10,000 rows runs in batches of 1,000 with a pause; a single statement locked replication once
f|the search index rebuilds from the database nightly; a document missing from search is at most a day old
m|a write-heavy table on the same disk as the log made every commit wait; the log has its own volume
p|analysts prefer read replicas with a 15-minute lag window that they know over live reads that they cannot predict
f|row-level security enforces tenant isolation in the multi-tenant tables; the application sets the tenant on connect
s|tenant-isolation-test: runs the query suite as one tenant and asserts nothing from another tenant appears
m|an ORM loaded a customer's 40,000 orders to compute a total; the total is a SQL aggregate now
c|optional data goes in a typed JSON column with a documented schema, not in a new table per optional field
f|the time-series store keeps raw data for 30 days and hourly rollups for two years
m|a hot row for the daily counter serialized every write; the counter is sharded across 16 rows and summed
p|the platform team prefers a managed queue over using the database as a queue above 100 jobs a minute
f|the archive database holds records older than 7 years in cold storage and answers a query within a day
s|orphan-rows: finds rows whose parent was deleted before the foreign key was enforced
m|the collation differed between two databases and a join on email matched different rows on each
c|a new database starts from the hardened template with backups, monitoring and access logging already on

## queues | Messaging & queues | databases, observability, payments, data
c|every message carries a schema version and a message id; consumers ignore versions they do not know
f|a message is retried 5 times with backoff and then parked in the dead-letter queue with its error
m|two consumers in the same group processed the same message after a rebalance; processing is idempotent on message id
s|dlq-replay: inspects parked messages, groups them by error and replays a group after the fix ships
p|the platform team prefers at-least-once delivery with idempotent consumers over exactly-once promises
f|the topic retention is 7 days; a consumer down longer than that starts from the earliest surviving offset
m|a consumer committed offsets before processing and lost 2,000 messages in a crash; it commits after
c|topics are named domain, event, version; the producer owns the schema and publishes it to the registry
f|the maximum message size is 1 MB; anything larger goes to object storage with a pointer in the message
m|a producer with no key spread one customer's events across partitions and they arrived out of order
s|consumer-lag-report: shows lag per consumer group and partition, with the rate of change
p|the team prefers one consumer group per service; sharing a group across services made ownership unclear
f|events describe what happened in the past tense; a message that tells another service what to do is a command, and they are separate topics
m|a poison message crashed the consumer on every retry and blocked the partition for an hour; unparseable messages are parked immediately
c|consumers are deployed before producers when a schema changes; the reverse order broke deserialization twice
f|the outbox pattern publishes from the database transaction; a message and its database write cannot disagree
m|the scheduler and the queue both retried a failed job and ran it three times; one retry owner per job
s|topic-audit: lists topics with no consumer in 30 days and topics whose producer no longer exists
p|the team prefers small events with ids over fat events that carry the whole object
f|partition count is fixed at creation for ordering; the guideline is twice the expected consumer count
m|a batch consumer acknowledged the whole batch when one message failed; it acknowledges per message now
c|a topic is not deleted until every consumer group has been removed and its owners confirmed
f|the queue's visibility timeout is 5 minutes; a handler slower than that gets its message redelivered
m|the heartbeat interval was longer than the session timeout after a config change and every consumer was kicked every minute
s|message-trace: follows one message id across topics and services and shows where time was spent
f|compacted topics keep the latest value per key; they are the source for rebuilding a service's local cache
m|a replay of 40 million events for a rebuild was throttled by the consumer's own metrics exporter
p|the platform team prefers schema evolution with defaults over versioned topics for compatible changes
c|a consumer that falls more than an hour behind pages its owner; the queue itself pages nobody
f|priority is a separate queue, not a message field; a single queue cannot reorder what it already holds
m|the retry backoff was fixed at one second and a downstream outage produced a wall of retries; it is exponential with jitter
s|schema-compat-check: verifies a proposed schema change is readable by every consumer version in the field
f|messages are encrypted in transit and at rest; personal data inside a message is tokenized before publishing
m|an event with a customer's email as the key made the topic itself personal data under retention rules
c|a consumer is a pure function of the message and its own store; it does not call the producer back
p|the team prefers a scheduled job that reads a table over a delayed message for anything beyond an hour
f|the broker cluster survives one node loss without data loss; a two-node loss pauses writes rather than losing them
m|the message timestamp was the broker's, not the producer's, and a clock difference reordered events in analytics
s|throughput-test: publishes at a target rate against a staging topic and reports consumer lag and error rate
f|a topic's ownership, schema and consumers are listed in the catalog; an unlisted topic is deleted after 30 days

## testing | Testing & quality | cicd, frontend, api, databases
c|a bug fix comes with a test that failed before the fix; the reviewer checks the test was red
f|the unit suite runs in under 2 minutes; anything slower is moved to the integration stage
m|a mocked payment client returned success for every call and the test suite missed a real decline for a year
s|flaky-finder: reruns the suite's failures in isolation and lists tests that pass on rerun, with their history
p|the team prefers testing behavior through the public interface over asserting on internal calls
f|the integration environment resets between runs; a test that depends on leftover data is a defect
m|a test asserted on the exact error message and every wording improvement broke it; it asserts on the error code
c|test data is built by factories with sensible defaults; a test names only the fields it cares about
f|coverage is reported but not enforced; the last enforced threshold produced tests that asserted nothing
s|mutation-run: mutates the code under test and lists the mutants the suite failed to kill
m|the end-to-end suite ran against production data and created 400 test customers in the CRM
p|product prefers a walkthrough of the feature on a call over a written acceptance test
c|contract tests run on both sides of a service boundary; a provider change that breaks a consumer contract blocks the merge
f|the browser suite runs on the three supported engines; a failure on one is a failure
m|a time-dependent test broke on the first of every month; time is injected, never read from the clock
s|test-impact: selects the tests affected by a change from the coverage map and runs those first
f|performance tests run nightly against a fixed dataset; a 10% regression opens a ticket automatically
m|the load test hit the rate limiter and measured the limiter, not the service; test traffic carries a bypass token
c|a test has one reason to fail; a test with three assertions on three behaviors is three tests
p|the team prefers table-driven tests for parsers and validators over one test function per case
f|snapshot tests are allowed for rendered output only, and a snapshot update requires a reviewer to look at the diff
m|a test suite passed for six months with a skip annotation nobody remembered; skips carry an expiry date now
s|coverage-gaps: lists changed lines in a pull request not executed by any test
f|the accessibility checks run in the component tests; a contrast failure fails the test
m|a race condition reproduced once in a thousand runs; the stress harness runs the test 2,000 times on demand
c|external services are replaced by recorded responses in tests, and the recordings are refreshed monthly
p|the reliability team prefers chaos experiments in staging on a schedule over ad hoc ones in production
f|a test environment is created per pull request and destroyed on merge; the cost is about €2 per pull request
m|two tests shared a database row and passed alone but failed together; every test owns its rows
s|test-data-refresh: rebuilds the anonymized test dataset from production shapes without production values
f|the fuzzer runs weekly against the parsers and has found four crashes in a year, all in the date parser
m|the test for a retry was asserting on sleep calls and passed when the retry logic was deleted
c|a test name states the behavior and the condition; a test called test-1 is renamed in review
p|the team prefers a failing test over a ticket for a reported bug; the test is the ticket
f|test doubles for the payment provider are shared in one package; every team used to have its own, and they disagreed
m|the screenshot tests differed by one pixel between operating systems; they run in one container image now
s|regression-pack: assembles the tests from every incident's postmortem into one suite that runs before a release
f|property-based tests cover the money arithmetic; the example tests missed rounding on negative amounts
m|the suite's run time doubled in a quarter from unbatched database setup; setup runs once per file now
c|a quarantined flaky test is fixed or deleted within two weeks; the quarantine list is reviewed on Mondays

## devenv | Developer environment | cicd, security, cloud, testing
c|the development environment is created by one command from the repository; a step in a wiki page is a defect
f|a fresh clone builds in about 6 minutes; the container image with dependencies pre-installed cuts it to one
m|the setup script assumed a case-insensitive filesystem and broke on Linux for a month before anyone said so
s|env-doctor: checks tool versions, credentials and network access and prints the fix for each failure
p|the team prefers the editor's built-in language server settings checked into the repository over personal configuration
f|tool versions are pinned in one file at the repository root and the version manager reads it
m|two engineers had different formatter versions and reformatted each other's files on every commit
c|pre-commit hooks format and lint; they do not run tests, because a slow hook gets bypassed
f|the local database is seeded with the anonymized dataset; production data never reaches a laptop
s|branch-cleanup: lists merged and stale branches older than 30 days and deletes them on confirmation
m|the local service discovery pointed at staging by default and a developer's test wrote to staging
p|the platform team prefers remote development containers for new hires and lets everyone else choose
f|secrets for local development come from the vault through the CLI login; the shared file of keys was deleted
m|a laptop with the full repository history took 20 minutes to clone; partial clone brings it to two
c|generated files are not committed; the build generates them and the ignore file keeps them out
f|the monorepo's tooling builds only the affected packages; a full build is a nightly job
s|dep-upgrade: upgrades one dependency, runs the affected tests and opens a pull request with the changelog
m|the debugger could not attach through the container until the port was published; the template publishes it
p|the team prefers a make-style task runner with a help target over remembering command lines
f|the code owners file routes reviews; a directory without an owner cannot be merged into
m|a proxy setting in the corporate network broke package downloads and the fix was documented in four places, all different
c|documentation for a tool lives next to the tool; the wiki page points there and says nothing else
f|the shared development cluster is rebuilt every Sunday; anything not in the definition is gone
s|new-service: scaffolds a service with the pipeline, the observability wiring and the health endpoint from the template
m|the template's health endpoint returned 200 before the service was ready and the rollout advanced too early
p|the team prefers small focused pull requests with a description of the reasoning over large ones with a list of files
f|a pull request is reviewed within one working day or the author is told when it will be
m|the editor plugin that auto-imported picked the wrong module of the same name in four pull requests
c|commit messages state why, not what; the diff already states what
f|the review checklist is a template in the pull request description, and an empty checklist is flagged
s|pr-summary: drafts the pull request description from the diff and the linked ticket, for the author to edit
m|a global git ignore on one laptop excluded a directory the build needed and the pipeline failed only for that person
p|new engineers prefer a first task that ships on day one over a week of reading
f|the developer portal lists every service with its owner, its runbook and its dashboards
m|the local stack needed 12 GB of memory after every service was added; a profile starts only the services a task needs
c|a branch is rebased on main before review; a merge commit in a feature branch is squashed away
f|the license checker runs on every dependency change and blocks copyleft licenses in the service code
s|onboarding-check: verifies a new engineer has every access and tool the role needs and lists what is missing
m|the emulator for the payment terminal did not exist and every test needed a real device until one was written
p|the team prefers a short recorded walkthrough for each service over a long written overview

## ml | Machine learning & LLM ops | data, analytics, security, cloud
f|the fraud model is retrained weekly on the trailing 90 days and promoted only if it beats the champion on the holdout
m|training data included the label through a joined column and the model scored perfectly until it met production
c|every model in production has a card naming its data, its metrics, its owner and its known failure modes
s|drift-monitor: compares the live feature distributions against the training set and alerts on divergence
p|the ML team prefers a simple model with monitoring over a complex model without it
f|inference latency budget is 50 ms at p99 for the ranking model; the transformer variant needed distillation to fit
m|the feature pipeline computed a feature differently online and offline and the model degraded silently for a month
c|features are defined once in the feature store and served to training and inference from the same definition
f|the language model's context window is 128k tokens; anything beyond 60k degrades on the retrieval benchmark
s|eval-suite: runs the prompt against the golden set and reports accuracy, refusal rate and cost per query
m|a prompt change that improved one task regressed another; every prompt change runs the full evaluation now
p|the team prefers retrieval over fine-tuning for knowledge that changes weekly
f|embeddings are versioned with the model that produced them; a model change re-embeds the whole corpus
m|the retrieval index returned chunks with no source, and the assistant cited documents that did not exist
c|generated text shown to customers is labeled as generated and carries a link to the sources it used
f|the GPU pool is shared; training jobs declare a maximum runtime and are preempted by inference under load
s|prompt-regression: replays production queries against a candidate prompt and diffs the answers
m|a temperature of 0 still produced different outputs across runs because of batching; tests allow semantic equivalence
p|product prefers the assistant to say it does not know over a confident guess, and the evaluation scores that
f|the classifier's threshold is set for 95% precision on the review queue; recall is what the human reviewers add
m|a model trained on last year's data missed a new product line entirely; training windows are checked against launches
c|training runs log their configuration, data snapshot and metrics; a run that cannot be reproduced is not promoted
f|model files are stored by content hash; the same weights uploaded twice occupy one object
s|cost-per-query: attributes token spend to features and tenants for the month, with the trend
m|the assistant followed instructions embedded in a retrieved document; retrieved text is marked as data, not instructions
p|the security team prefers a separate model endpoint per data classification over one endpoint with filters
f|the rate limit on the language model provider is 10,000 requests a minute; the client queues above 8,000
m|a tokenizer change in a provider update shifted the token counts and the cost forecast by 12%
c|human review of a sample of model outputs happens weekly, and the sample is stratified by confidence
f|the recommendation model is evaluated on a holdout that is time-split, not random; random splits leaked the future
m|a fine-tuned model memorized a customer's document and reproduced it for another customer; fine-tuning data is reviewed for personal data
s|bias-audit: reports the model's error rate across the protected groups the policy names
f|the evaluation set has 1,200 items labeled by two people each; disagreements are resolved by a third
m|batch inference was scheduled at midnight and competed with the data pipeline for the same cluster; it runs at 04:00
p|the ML team prefers a model registry with staged promotion over deploying from a notebook
c|a model is retired when its replacement has served 100% of traffic for two weeks without a rollback
f|structured output from the language model is validated against a schema and retried once on failure
m|the assistant's answer quality dropped when a retrieval component was upgraded; the evaluation runs on every dependency change
s|hallucination-check: verifies every citation in a generated answer resolves to a retrieved source
f|the classifier explains its decision with the top three features, and the explanation is stored with the decision

## analytics | Product analytics & experiments | data, frontend, ml, support
c|events are named object-action in past tense; an event not in the schema registry is dropped at ingestion
f|the daily active users metric counts a user once per UTC day; the product team's local-day metric differs by up to 6%
m|the sign-up funnel counted page views instead of unique users and reported a 140% conversion on one step
s|funnel-report: computes conversion between named steps for a cohort and shows the drop-off with confidence intervals
p|the analytics team prefers event properties on the event over joining to the user table at query time
f|an experiment needs 2 weeks and 20,000 users per arm to detect a 2% relative change on the primary metric
m|an experiment was stopped on day 3 with a significant result that reversed by day 10; the minimum duration is enforced
c|every experiment registers its primary metric and its sample size before it starts; peeking is reported
f|the consent state is a property on every event; events without consent are dropped, not stored and filtered later
s|experiment-readout: produces the readout for a finished experiment with the pre-registered metrics first
m|two dashboards computed revenue per user with and without refunds and the difference was called a bug three times
p|product prefers a small set of guardrail metrics on every experiment over a long list of secondary metrics
f|session timeout is 30 minutes of inactivity; a change to 15 would raise the session count by 18%
m|a bot crawling the pricing page inflated page views by 40% for a week; known bots are filtered at ingestion
c|dashboards for the leadership review are frozen on Monday at 09:00 and their numbers are not restated
f|the attribution window for marketing is 7 days click and 1 day view; the vendor's default was 28 days
s|metric-diff: explains the difference between two versions of a metric by decomposing the definitions
m|a client-side event fired before the page finished loading and undercounted slow connections by a fifth
p|the growth team prefers holdout groups that run for a quarter to measure the cumulative effect of a program
f|the sample ratio mismatch check runs daily on every experiment; a mismatch pauses the experiment
m|a feature flag and an experiment targeted the same users and the experiment measured the flag
c|an experiment's exposure event fires when the user sees the difference, not when they are assigned
f|retention is measured on the day-7 return for the acquisition cohort; day-1 was too noisy to guide decisions
s|cohort-builder: defines a cohort by events and properties and exports it to the messaging tool
m|the churn model's top feature was the cancellation survey, which is answered after the decision
p|the analytics team prefers one source of truth for the customer list that every tool syncs from
f|the event volume is 400 million a day; sampling at 10% is applied to page views and never to transactions
m|a currency mix shift made revenue look flat while unit sales grew 15%; revenue is reported in constant currency
c|a metric's definition changes with a version and a date; dashboards show which version they use
f|the north-star metric is weekly active teams with three or more members who completed a core action
s|anomaly-scan: checks every top-level metric against its forecast each morning and lists the surprises
m|a new onboarding step improved activation and reduced week-4 retention; the readout carried both
p|product prefers qualitative interviews with five users before an experiment over a survey with five hundred
f|the experimentation platform assigns by a hash of the user id and the experiment key; assignment is stable across devices after login
m|the pricing page test ran in one currency and the result did not transfer to the others
c|personal data does not enter the analytics warehouse; the user id is a pseudonymous key rotated yearly
f|a p-value threshold of 0.05 with 20 metrics produces a false positive per experiment; the primary metric is what decides
s|event-validator: checks a build's emitted events against the schema registry in the test environment
m|the mobile app batched events and sent them hours later; event time is the device time, receipt time is separate
f|the weekly business review reads from one dashboard with definitions linked on every tile

## compliance | Compliance & privacy | security, payments, governance, data
f|personal data is retained for the contract term plus 3 years, then deleted; the retention job runs monthly and reports what it removed
m|an export for a data access request missed the support tickets because they lived in a vendor system; the export map lists every system
c|every new data flow is recorded in the processing register before it goes live; the register is reviewed quarterly
s|access-request: assembles a person's data from every system in the export map into one package within the deadline
p|legal prefers data residency per region with the customer choosing at sign-up over a single global region
f|a data breach affecting individuals is reported to the regulator within 72 hours of confirmation
m|a subprocessor was added to the platform without updating the customer notice; changes go through legal first now
c|consent is recorded with the text shown, the version and the time; a consent without its text is not consent
f|the audit for the security certification takes 6 weeks each year and needs evidence for every control
s|evidence-collector: gathers the audit evidence for each control from the systems that hold it and files it by control
m|the anonymization kept the postal code and birth date and the dataset was re-identifiable for a third of rows
p|the compliance team prefers automated controls that produce their own evidence over quarterly manual attestations
f|the vendor review covers security, privacy and continuity; a vendor with production data access is reviewed yearly
m|a retention policy deleted invoices the tax authority required for 10 years; financial records have their own schedule
c|deletion requests are honored across backups within 90 days; the backup job excludes deleted keys on restore
f|customer data leaves its region only with a documented transfer mechanism, and the list of mechanisms is legal's to change
s|dpia-template: produces the impact assessment questions for a new feature from its data description
m|a marketing tool synced the customer list including people who had opted out; sync respects the consent field now
p|the team prefers to collect less data over protecting more; a field without a documented use is removed
f|the accessibility statement is reviewed twice a year and lists the known gaps with their dates
m|an employee's access remained active for 40 days after leaving; deprovisioning runs the same day now
c|the terms of service change with 30 days' notice and a diff; the notice email links to the diff
f|the data protection officer is named on the privacy page and answers within 5 working days
s|retention-report: lists every data store with its retention schedule, its last purge and any overdue purge
m|the cookie banner set analytics cookies before consent; the consent check runs before any script loads now
p|legal prefers records of decisions with the reasoning kept, even when the decision is reversed
f|the incident register records every incident affecting personal data, whether or not it met the reporting threshold
m|a shared spreadsheet of customer contacts was found in a personal drive; personal data lives in named systems only
c|privacy review happens at design, not at launch; a feature reviewed after build was rebuilt twice
f|the age of consent for the service is 16; accounts of younger users need a verified parental consent
s|subprocessor-check: compares the deployed vendors against the published subprocessor list and flags the difference
m|the export format for portability was a PDF; it is machine-readable now, as the regulation requires
p|the compliance team prefers a single policy library with owners and review dates over documents scattered by team
f|training on data handling is completed within 30 days of joining and renewed yearly; completion is tracked
m|a customer's deletion request was completed in the product but their data survived in the analytics warehouse for a year
c|a legal hold suspends deletion for the named records only; a blanket hold on everything is refused
f|the records of processing list the lawful basis for each purpose; a purpose without a basis is stopped
s|consent-audit: samples recorded consents and verifies each has its text version and matches the current notice
m|the contract template promised a deletion timeline the systems could not meet; the template matches the runbook now
f|the whistleblowing channel is external, anonymous and answered within 7 days

## support | Customer support & operations | payments, observability, product, compliance
f|first response on a paid plan is within 4 business hours; the median last quarter was 52 minutes
m|the escalation path to engineering was a chat message; escalations go through the ticket with a severity and an owner now
c|every ticket carries the tenant, the plan and the product area before it is assigned
s|ticket-triage: classifies incoming tickets by area and urgency and drafts the first response for the agent to edit
p|support prefers one shared inbox per product area over per-person queues
f|the knowledge base article is linked in the first response for the top 20 questions, which are 60% of volume
m|a status page incident was posted 40 minutes after customers noticed; the alert now opens the draft automatically
c|a customer is told what happened and what changes, in plain words, within 5 days of an incident affecting them
f|refunds under €50 are approved by the agent; above that, the team lead, and above €500, finance
s|customer-context: assembles a tenant's plan, recent incidents, open tickets and recent changes for the agent on one screen
m|a macro reply was sent to a customer whose issue it did not address; macros are inserted, not sent, since then
p|customers prefer a direct answer with the workaround first and the explanation second
f|the support tooling logs every account access by an agent with the ticket id; accesses without a ticket are reviewed weekly
m|the same bug produced 300 tickets before it was linked to one issue; tickets link to the tracked issue now
c|feature requests are recorded against the roadmap item with the customer and the reason, not as free text
f|the help center is searched 20,000 times a month; the top zero-result queries are reviewed monthly
s|zero-result-report: lists help center searches that returned nothing, grouped by intent
m|a customer's request to delete their account was handled in the product but not in the billing system, which kept charging
p|the support team prefers a callback within an hour over a queue for anything blocking a customer's payment
f|the customer satisfaction survey goes out on ticket close and is answered by a third of customers
m|the survey link expired after 24 hours and customers in other time zones could not answer; it lives 7 days
c|an agent can read a customer's data and cannot change it; changes go through the customer's own account or a signed request
f|the on-call engineer answers a severity-1 support escalation within 15 minutes, at any hour
s|incident-comms: drafts the status page update and the customer email from the incident record
m|a workaround posted in the help center outlived the fix by a year and sent customers down the wrong path; articles carry a review date
p|the product team prefers to read ten raw tickets a week over a summary of a thousand
f|enterprise customers have a named contact and a quarterly review; the review covers incidents, usage and the roadmap
m|the customer's admin was not told about a security notice sent to the billing contact; security notices go to both
c|a ticket is closed by the customer or after 7 days of silence following a resolution; never by the agent alone
f|support hours are 06:00 to 20:00 in the customer's region; outside those, the severity-1 line
s|sla-report: computes response and resolution time against the plan's promise per tenant per month
m|an agent shared a screenshot with another customer's data visible; the tooling masks other tenants in screenshots now
p|support prefers the release notes written for customers by the product team, not adapted from the engineering changelog
f|a bug affecting more than 10 tenants opens a tracked incident, even if none of them have written in
m|the chat widget promised a 2-minute response outside support hours; it shows the hours and the queue now
c|the customer's language is detected from their account setting; a reply in the wrong language is a defect
f|refund decisions are recorded with the reason from the fixed list, and the monthly report is built from it
s|churn-signals: flags accounts with rising ticket volume, failed payments or dropping usage for the account manager
m|an escalation to engineering had no reproduction steps and bounced three times; the triage template requires them
f|the support team's runbooks are in the same repository as engineering's, and a change is reviewed by both

## product | Product & roadmap | analytics, support, governance, frontend
c|a feature is described by the problem it solves and how success is measured before any design starts
f|the roadmap is published quarterly with themes, and a dated commitment appears only when the work has started
m|a feature shipped to every customer on day one and support was flooded; new features roll out to 5% first
s|feedback-digest: groups the month's customer feedback by theme and links each theme to the roadmap item or opens one
p|the product team prefers to remove a feature used by under 1% of accounts over maintaining it
f|pricing changes are announced 60 days ahead to existing customers and apply at their next renewal
m|the beta label stayed on a feature for two years and enterprise buyers refused to rely on it; betas end within two quarters
c|every customer-facing change has release notes written for the customer, with a screenshot when the interface changes
f|the discovery interview script is 6 questions about the customer's last week, not about the proposed feature
s|opportunity-sizing: estimates the accounts and revenue affected by a proposed change from usage data
m|a feature requested by the loudest customer was used by that customer alone; requests are weighed by accounts, not volume
p|design prefers to prototype in the product with a flag over a separate clickable mock
f|a deprecation gives 6 months' notice with a migration path; a deprecation without a path is refused
m|the roadmap page promised a date the engineering plan never had; dates come from the plan and nowhere else
c|the problem statement, the metric and the decision record for each feature live with the feature's documentation
f|onboarding completion is the activation metric; it is 61% and the target for the quarter is 70%
s|adoption-report: shows a feature's usage by account segment since launch against its target
m|a settings page grew to 84 options; settings without a documented reason were removed and 60 remained
p|product prefers defaults that work for most customers over options that let every customer configure
f|the customer advisory board meets quarterly with 12 accounts across segments; notes are published internally within a week
m|the free plan's limits were raised to reduce support tickets and paid conversion fell by a third
c|a feature flag has an owner and a removal date; a flag older than 90 days is a ticket for its owner
f|the design review covers accessibility, localization and the empty state before the visual polish
s|competitor-changes: summarizes competitors' public release notes for the month against our roadmap
m|the pricing page offered four tiers and customers chose the second cheapest regardless of fit; three tiers converted better
p|the product team prefers to ship the smallest version that a customer can complete a task with
f|customer-reported bugs are triaged within one working day and the customer is told the outcome
m|an integration was built for a partner who then changed their API; partner integrations carry a contract for notice
c|the product review reads the metrics before the demo; a demo without metrics is rescheduled
f|the localization backlog is prioritized by revenue per language; the top four languages cover 90% of revenue
s|release-readiness: checks documentation, support briefing, release notes and rollout plan for a feature before launch
m|a change to the default sort order was shipped without notice and 200 customers wrote in the first day
p|the team prefers a public changelog with dates over a marketing announcement for each release
f|the product principles are five sentences on one page, and a decision that contradicts one is escalated
m|a large customer asked for a custom feature; it was built as a general feature and the customer's version was refused
c|a request for a feature that exists is a documentation defect, and it is tracked as one
f|enterprise contracts name the features included; a feature not on the list is not promised by sales
s|usage-cohorts: compares retention of accounts that adopted a feature against those that did not, matched by segment
m|the trial length was cut from 30 days to 14 and conversion rose because customers decided sooner
f|the product team reviews the top ten support themes every month with the support lead in the room

## governance | Knowledge governance | compliance, security, product, ml
c|nothing an agent writes down is shared beyond its author until a reviewer that did not write it has signed it
f|a rejected submission stays in the record with its reasons; it is not deleted and it is never readable
m|a wrong retry rule propagated to every agent on a team before anyone questioned it; it was a fact for three weeks
s|provenance-export: produces the signed chain for an artifact from submission to every approval, for an auditor
p|the governance team prefers a contradiction left visible over a silent choice between two beliefs
f|a merged belief supersedes its parents; the parents stay in the record with the edges that connect them
m|two teams held opposite conventions for the same API and both were right for their own service; the record keeps both
c|the reviewer runs in its own process, without tools and without network, and did not write what it reviews
f|approval within a team is one signature; approval for the organization needs a second, independent one
s|gap-report: lists what a team's agents keep asking that no approved artifact answers
m|a skill installed from a public hub carried a credential exfiltration; scanning happens before review, not after
p|security prefers every artifact to carry the identity that wrote it and the identity that signed it
f|an artifact's identity is the hash of its content; the same claim submitted twice is one artifact
m|a summary written by an agent was treated as a fact by another agent; summaries are labeled as derived
c|a claim about a regulated matter is not approved without the citation that supports it
f|the audit trail is append-only and hash-chained; an auditor verifies it without trusting the operator
s|verdict-history: shows every decision on an artifact, who made it and what changed between versions
m|a memory that was correct in March was wrong by June; artifacts carry the date they were last confirmed
p|the organization prefers a belief with a known author over an anonymous one, even when the anonymous one is older
f|a connector that proposes knowledge from a ticketing system is an author; its proposals wait for review like anyone's
m|a convention was approved for one team and inherited by another where it did not apply; scope is part of the artifact
c|an artifact is revised by a new version that supersedes the old; editing in place is refused
f|the reviewer's grade has axes: correctness, specificity, safety and scope; a verdict names its scores
s|review-queue: shows what is waiting for a human reviewer, ordered by age and by how many agents asked for it
m|a review queue of 300 items meant nothing was reviewed; the queue is capped and the oldest items age out with notice
p|reviewers prefer to see the claim's neighbors in the record before grading it
f|a reviewer that grades its own writing is a configuration error and the system refuses to start
m|a preference held by one engineer was promoted as a team convention; preferences and conventions are separate kinds
c|the reasons for a rejection are written for the author to act on; a rejection without reasons is a defect
f|an artifact readable by the organization was readable by its team first, and by its author before that
s|scope-check: lists artifacts readable at a wider scope than any approval granted them
m|a public skill's update changed its behavior after approval; approvals bind to the content hash, not the name
p|the platform team prefers the gate to sit on the write path; a read-time filter was rejected as a design
f|every verdict is signed with the reviewer's key, and the key belongs to an identity in the register
m|a merge of two beliefs was proposed by the same component that graded it; proposers and deciders are separate identities
c|a claim's scope names the systems it applies to; a claim without a scope is asked for one before review
f|a human can override the automated reviewer; the override is signed and carries its reason
s|claim-neighbors: finds the approved artifacts closest to a new claim and shows where they agree and where they differ
m|an agent reported that its folder was missing a rate-limit rule; the report became the first entry in the gap register
f|the record is exportable in full; an organization leaving takes its signed history with it
